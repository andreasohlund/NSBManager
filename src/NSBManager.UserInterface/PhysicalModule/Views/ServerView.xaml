<UserControl x:Name="ServerViewControl" x:Class="NSBManager.UserInterface.PhysicalModule.Views.ServerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:commands="clr-namespace:Microsoft.Practices.Composite.Presentation.Commands;assembly=Microsoft.Practices.Composite.Presentation">
    <UserControl.Resources>
       
    </UserControl.Resources>

    <Grid HorizontalAlignment="Stretch">
        <ListView ItemsSource="{Binding Path=Servers}"
                  SelectedItem="{Binding Path=SelectedServer, Mode=TwoWay}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Server Name" DisplayMemberBinding="{Binding Path=Name}" />
                </GridView>
            </ListView.View>
        </ListView>
        
    </Grid>
</UserControl>
