<UserControl x:Class="NSBManager.UserInterface.Views.ShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:UserInterface="clr-namespace:NSBManager.UserInterface"
             Width="800"
             Height="600">
    <StackPanel Background="{StaticResource Shell Background}">
        <DockPanel>
            <UserInterface:NSBMLogo />
            <ContentControl x:Name="MainMenu"
                            DockPanel.Dock="Top"
                            Margin="20 10" />
        </DockPanel>
        <StackPanel Margin="20 10"
                    MinHeight="200">
            <ContentControl x:Name="ActiveScreen">
                <TextBlock Text="Testing"
                         Foreground="White"
                         FontSize="25" />

            </ContentControl>
        </StackPanel>
    </StackPanel>
</UserControl>
