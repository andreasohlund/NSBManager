<UserControl x:Class="NSBManager.UserInterface.Views.ShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:UserInterface="clr-namespace:NSBManager.UserInterface"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
             MinWidth="800"
             MinHeight="600">
    <StackPanel Background="{StaticResource Shell Background}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <DockPanel>
            
            
            <UserInterface:NSBMLogo />
            
            <ContentControl x:Name="MainMenu"
                            DockPanel.Dock="Top"
                            Margin="20 10" />
        </DockPanel>
        <StackPanel Margin="20 10"
                    MinHeight="200">
            <ContentControl x:Name="ActiveScreen">
                <TextBlock Text="Testing"
                         Foreground="White"
                         FontSize="25" />

            </ContentControl>
        </StackPanel>
    </StackPanel>
</UserControl>
